export enum FormsItemType {
    Input = 'input',
    Select = 'select',
    Checkbox = 'checkbox',
    Radio = 'radio',
    TextArea = 'textarea',
    Date = 'date',
    Number = 'number',
    File = 'file',
}
