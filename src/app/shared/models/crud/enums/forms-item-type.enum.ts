export enum FormsItemType {
    TEXT = 'text',
    NUMBER = 'number',
    DATE = 'date',
    TEXTAREA = 'textarea',
    CHECKBOX = 'checkbox',
    RADIO = 'radio',
    FILE = 'file',
    SEARCH = 'search',
    SELECT = 'select'
}
