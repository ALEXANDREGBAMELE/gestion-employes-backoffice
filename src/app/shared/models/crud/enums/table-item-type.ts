export enum TableItemType {
    Text = 'text',
    Bool = 'bool',
    Custom = 'custom',//POUR AFFICHER LES ELEMENTS AVEC LEURCLASSE
    Number = 'number',
    Concat = 'concat' //POUR AFFICHER LES ELEMENTS CONCATENEE DANS LE TABLEAU
}
